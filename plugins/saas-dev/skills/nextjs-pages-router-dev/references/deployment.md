# Next.js Pages Router: Deployment

Same as App Router deployment — Vercel zero-config or Docker with standalone output.
Same `next.config.ts` with `output: 'standalone'` for Docker.
Same Django CORS rules — allow only Next.js server origin.
See App Router deployment reference for full Docker setup (identical).

---